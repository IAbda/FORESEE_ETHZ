def train_stuff():
    """Trains all the things
    """

    with open("trained_model.txt", "w") as fp:
        fp.write("[melody of beyonce] all the models, this is the model, la la la la lah-laaaaa")


if __name__ == "__main__":

    train_stuff()
